library(shiny)
library(ggplot2)

ui <- fluidPage(
  titlePanel("Affichage des logements neufs et anciens"),
  
  sidebarLayout(
    sidebarPanel(
      h4("Informations sur les logements"),
      p("Les cinq premières lignes des logements neufs et anciens")
    ),
    
    mainPanel(
      h3("Logements Neufs"),
      tableOutput("neufs_table"),
      
      h3("Logements Anciens"),
      tableOutput("anciens_table")
    )
  )
)
